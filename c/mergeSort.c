#include <stdio.h>
#include <stdlib.h>
#include "myfunctions.c"

int array[100];


void mergeSort(int array[]){
    int middle = len(array)/2 + 1;

    if(len(array) == 1){
        return;
    }

    mer
}

void merge(){

}

int main(int argc, char* argv){
    shuffleIntArray(array, len(array));
    printIntArray(array, len(array));
    return 0;
}